# Copy this file to .env (same folder). No quotes around values.
# Get key: https://console.x.ai/team/default/api-keys
XAI_API_KEY=xai-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
JWT_SECRET=any-long-random-string-at-least-32-chars
XAI_CHAT_MODEL=grok-3-mini
PORT=3000
HOST=0.0.0.0

# PostgreSQL (local example)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mia

# Railway Bucket — reference from bucket service (AWS SDK preset) on companion.
# Local dev: leave unset to store voice files in server/data/uploads/
# BUCKET=my-bucket-xxxxx
# ENDPOINT=https://storage.railway.app
# REGION=auto
# ACCESS_KEY_ID=
# SECRET_ACCESS_KEY=

# Legacy disk path for voice when bucket is not configured
# DATA_DIR=/data
