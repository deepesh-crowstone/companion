/// Non-web platforms have no Google Ads `gtag`, so conversion reporting is a
/// no-op here. The web implementation lives in `ads_conversion_service_web.dart`.
void trackConversion(String sendTo) {}
